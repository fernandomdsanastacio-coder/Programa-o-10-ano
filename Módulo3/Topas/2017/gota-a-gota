
#include <iostream>
#include <string>
#include <locale.h>
#include <cstdlib>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Portuguese");
	int m, M, A, B, D, j = 0, aux = 0, D1 = 0, D2 = 0, D3 = 0;
	string C;
	cin >> m >> M >> A >> B;
	cin >> D;
	
	for (int i = 0; i < D; i++)
	{
		aux = 0;
		cin >> C;
		for (j=0;j<C.size();j++)
		{
			if (C[j] == '1')
			{
				aux++;
			}
		}
		if (aux < m)
		{
			D1++;
		}
		else if (aux > M)
		{
			D2++;
		}
		else if (aux >= A && aux <= B)
		{
			D3++;
		}
	}
	cout << D1 <<endl <<D2 << endl << D3<<endl;
	return 0;
	
		
}
